import styled from 'styled-components'; 
  
 
export const Button = styled.div` 
   position: fixed;  
   width: 40px; 
   left: 50%; 
   bottom: 40px; 
   height: 40px; 
   font-size: 3rem; 
   z-index: 1; 
   cursor: pointer; 
   color: #A53DFF;
   filter: blur(2px);
`